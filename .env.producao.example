# =============================================================================
# MockMail.dev - Variáveis de Ambiente (Produção)
# =============================================================================
# ⚠️  ATENÇÃO: Este é um arquivo de EXEMPLO
#
# 1. Copie para .env: cp .env.producao.example .env
# 2. Substitua TODOS os valores por senhas seguras
# 3. NUNCA commite o arquivo .env no git
# 4. Use um gerenciador de segredos em produção (Vault, AWS Secrets, etc.)
# =============================================================================

# Ambiente
NODE_ENV=production
ENVIRONMENT=producao

# Versão (para tags das imagens Docker)
VERSION=1.0.0

# =============================================================================
# MONGODB
# =============================================================================
# Use uma senha forte! Exemplo: openssl rand -base64 32
MONGO_PASSWORD=SUBSTITUA_POR_SENHA_SEGURA_MONGODB

# =============================================================================
# REDIS
# =============================================================================
# Use uma senha forte! Exemplo: openssl rand -base64 32
REDIS_PASSWORD=SUBSTITUA_POR_SENHA_SEGURA_REDIS

# =============================================================================
# POSTGRESQL
# =============================================================================
# Use uma senha forte! Exemplo: openssl rand -base64 32
POSTGRES_PASSWORD=SUBSTITUA_POR_SENHA_SEGURA_POSTGRES

# =============================================================================
# JWT (CRÍTICO - Segurança da Autenticação)
# =============================================================================
# Gere chaves únicas! Exemplo: openssl rand -base64 64
JWT_SECRET=SUBSTITUA_POR_CHAVE_JWT_MUITO_SEGURA_COM_PELO_MENOS_64_CARACTERES
JWT_REFRESH_SECRET=SUBSTITUA_POR_CHAVE_REFRESH_MUITO_SEGURA_COM_PELO_MENOS_64_CARACTERES
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# =============================================================================
# CSRF (CRÍTICO - Proteção contra CSRF)
# =============================================================================
# Gere uma chave única! Exemplo: openssl rand -base64 32
CSRF_SECRET=SUBSTITUA_POR_CHAVE_CSRF_SEGURA

# =============================================================================
# URLs
# =============================================================================
NEXT_PUBLIC_API_URL=https://api.mockmail.dev
CORS_ORIGIN=https://mockmail.dev
ALLOWED_ORIGINS=https://mockmail.dev,https://api.mockmail.dev

# =============================================================================
# COOKIES
# =============================================================================
COOKIE_DOMAIN=mockmail.dev
COOKIE_SECURE=true
COOKIE_SAME_SITE=strict

# =============================================================================
# LOGS
# =============================================================================
# Em produção, use 'info' ou 'warn' (não use 'debug')
LOG_LEVEL=info

# =============================================================================
# EMAIL PROCESSING
# =============================================================================
EMAIL_RETENTION_DAYS=30
MAX_EMAILS_PER_BOX=1000
MAX_BOXES_PER_USER=50

# =============================================================================
# RATE LIMITING
# =============================================================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=20

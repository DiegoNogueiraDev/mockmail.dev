# Server Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Configuration (Primary Database via Prisma)
DATABASE_URL=postgresql://mockmail:mockmail_dev_2026@localhost:5432/mockmail

# MongoDB Configuration (Legacy - will be deprecated)
MONGODB_URI=mongodb://app_user:YOUR_PASSWORD@localhost:27017/mockmail?authMechanism=SCRAM-SHA-256&authSource=mockmail&retryWrites=true&w=majority

# Redis Configuration (Sessions, Cache, Rate Limiting)
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# CSRF Configuration
CSRF_SECRET=your-csrf-secret-key-change-in-production
CSRF_COOKIE_NAME=mockmail_csrf_token

# Cookie Configuration
COOKIE_DOMAIN=localhost
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax

# CORS Configuration
ALLOWED_ORIGINS=https://mockmail.dev,https://watch.mockmail.dev,http://localhost:3000,http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=5

# Email Processing
EMAIL_RETENTION_DAYS=30
MAX_EMAILS_PER_BOX=1000
MAX_BOXES_PER_USER=50
